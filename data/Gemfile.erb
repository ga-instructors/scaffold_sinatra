source 'https://rubygems.org'

ruby '<%= RUBY_VERSION %>'

# Gems used in every environment
gem 'sinatra', '1.4.5'
<%= "gem 'dotenv',  '~> 0.9.0'" if full_scaffold? %>

# gems specific to the development environment
group :development do
  gem 'sinatra-contrib', '~> 1.4.2', require: 'sinatra/reloader'
  gem 'rake', '~> 10.3.2'
end

# gems specifically for testing
group :test do
end

# gems used in the local development and testing environments
group :development, :test do
  gem 'pry', '0.10.1'
end

# gems specific to the production environment
group :production do
end
