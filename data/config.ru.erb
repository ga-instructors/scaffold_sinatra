<% if standard_scaffold? %>
require 'rubygems'
require 'bundler'

Bundler.require(:default, ENV['RACK_ENV'])

<% else %>
require 'sinatra/base'
<% end %>

require_relative 'server'

run <%= project_camel_case %>::Server
